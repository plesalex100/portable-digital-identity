meta {
  name: checkpoints
}

vars:pre-request {
  baseUrl: http://localhost:3000
}

docs {
  # Checkpoints API

  Base path: `/api/checkpoints`

  Endpoints for interacting with the Checkpoints data from MongoDB.
}
